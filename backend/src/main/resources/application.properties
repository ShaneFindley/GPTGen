# Database configuration for client information
spring.datasource.client.url=jdbc:postgresql://shard1-host:5432/client_database
spring.datasource.client.username=your_username
spring.datasource.client.password=your_password
# ... other client database properties

# Database configuration for GPX datasets
spring.datasource.gpx.url=jdbc:postgresql://shard2-host:5432/gpx_database
spring.datasource.gpx.username=your_username
spring.datasource.gpx.password=your_password
# ... other gpx database properties

# Set the default language
spring.mvc.locale=default

# Localization properties
spring.messages.basename=i18n/messages
spring.messages.encoding=UTF-8
spring.messages.fallback-to-system-locale=false
